{{!--
Name: Joseph A Pfohl
Date: 11/30/2016
Class: CS290-400-F2016
Content of body for Single page application workout tracker
--}}

<h1 class="centered-text">Workout Tracker</h1>

{{#if reset}} {{! Only displays after visiting reset-table route}}
    <em><p class="reset-text" class="centered-text">{{reset}}</p></em>
{{/if}}

{{!-- Form for adding an exercise --}}
<div class="section add-form">
    <div class="container">
        <h2>Add an Exercise</h2>
        <h5 id="error-text"></h5>
        <form id="add-exercise-form">
            <div class="row">
                <div class="three columns">
                    <label for="exerciseName">Exercise Name</label>
                    <input class="u-full-width" type="text" name="name" id="exerciseName">
                </div>
                <div class="two columns">
                    <label for="repetitions">Repetitions</label>
                    <input class="u-full-width" type="number" name="reps" id="repetitions">
                </div>
                <div class="two columns">
                    <label for="weight">Weight</label>
                    <input class="u-full-width" type="number" name="weight" id="weight">
                </div>
                <div class="two columns">
                    <label for="unit">Unit</label>
                    <select class="u-full-width" name="unit" id="unit">
                        <option value="1">Pounds</option>
                        <option value="0">Kilograms</option>
                    </select>
                </div>
                <div class="three columns">
                    <label for="date">Date</label>
                    <input class="u-full-width" type="date" name="date" id="date">
                </div>
            </div>
            <div class="row">
                <div class="two columns offset-by-four">
                    <input id="add-button" class="u-full-width button-primary" type="submit" value="Submit">
                </div>
                <div class="two columns">
                    <button id="reset-button" class="u-full-width button" type="reset" value="Reset">Reset</button>
                </div>
            </div>
        </form>
    </div>
</div>

{{!-- Table for displaying rendered table of workouts --}}
<div class="section display-table">
    <div class="container">
        <h2>Completed Exercises</h2>
        <h5 id="row-error"></h5>
        <div class="row">

            <table id="complete-exercises" class="u-full-width">
                <thead>
                    <tr>
                        <th class="head-text">Exercise Name</th>
                        <th class="head-text">Repititions</th>
                        <th class="head-text">Weight</th>
                        <th class="head-text">Date</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id='exercise-list'>
                    {{!-- table rendered by JS will be here --}}
                </tbody>
            </table>

        </div>
    </div>
</div>

